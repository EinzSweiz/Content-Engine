apiVersion: v1
kind: Pod
metadata:
  name: content-engine-pod
  labels:
    name: content-engine-pod
spec:
  containers:
  - name: content-engine
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
